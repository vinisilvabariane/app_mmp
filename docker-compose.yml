services:
  web:
    container_name: app_mmp
    build: .
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - .:/var/www/html
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"

