document.addEventListener('DOMContentLoaded', function () {    'use strict'    const form = document.getElementById('education-interest-form')    const steps = Array.from(document.querySelectorAll('.wizard-step'))    const track = document.getElementById('wizard-track')    const prevBtn = document.getElementById('wizard-prev')    const nextBtn = document.getElementById('wizard-next')    const submitBtn = document.getElementById('wizard-submit')    const progressFill = document.getElementById('wizard-progress-fill')    const progressText = document.getElementById('wizard-progress-text')    if (!form || steps.length === 0 || !track || !prevBtn || !nextBtn || !submitBtn) {        return    }    let current = 0    const total = steps.length    const showToast = (type, message) => {        if (typeof toastr === 'undefined') {            console[type === 'error' ? 'error' : 'log'](message)            return        }        toastr.options = {            closeButton: true,            progressBar: true,            newestOnTop: true,            timeOut: 2600        }        toastr[type](message)    }    const validateStep = () => {        const step = steps[current]        const requiredFields = Array.from(step.querySelectorAll('input[required], textarea[required], select[required]'))        const radioNamesChecked = new Set()        for (const field of requiredFields) {            if (field.type === 'radio') {                if (radioNamesChecked.has(field.name)) continue                const checked = step.querySelector(`input[type="radio"][name="${field.name}"]:checked`)                if (!checked) {                    showToast('error', 'Lorem ipsum dolor sit amet.')                    return false                }                radioNamesChecked.add(field.name)                continue            }            if (field.type === 'checkbox') {                if (!field.checked) {                    showToast('error', 'Lorem ipsum dolor sit amet.')                    return false                }                continue            }            if (!String(field.value || '').trim()) {                field.focus()                showToast('error', 'Lorem ipsum dolor sit amet.')                return false            }        }        return true    }    const updateWizard = () => {        steps.forEach((step, index) => {            step.classList.toggle('active', index === current)            step.setAttribute('aria-hidden', index === current ? 'false' : 'true')        })        track.style.transform = `translateX(-${current * 100}%)`        const progress = ((current + 1) / total) * 100        if (progressFill) progressFill.style.width = `${progress}%`        if (progressText) progressText.textContent = `Lorem ${current + 1} de ${total}`        prevBtn.disabled = current === 0        const isLast = current === total - 1        nextBtn.classList.toggle('d-none', isLast)        submitBtn.classList.toggle('d-none', !isLast)    }    prevBtn.addEventListener('click', () => {        if (current > 0) {            current -= 1            updateWizard()        }    })    nextBtn.addEventListener('click', () => {        if (!validateStep()) return        if (current < total - 1) {            current += 1            updateWizard()        }    })    form.addEventListener('submit', (event) => {        event.preventDefault()        if (!validateStep()) return        showToast('success', 'Lorem ipsum dolor sit amet.')        form.reset()        current = 0        updateWizard()    })    updateWizard()})